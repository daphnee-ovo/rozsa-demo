pub async fn run() -> anyhow::Result<()> {
    // TODO:
    // 1. 解析 CLI 参数 → CreateSessionOptions
    // 2. create_session(options) → AgentSessionRuntime
    // 3. 根据模式启动 interactive / print
    Ok(())
}
