---
source: audit
nums: 1
---

- [ ] ISSUE-I072：App: file-mutation-queue 缺失 — 并发写同文件可能冲突
  - severity: P2
  - location：crates/rozsa-app/src/tools/write.rs
  - description：TS file-mutation-queue.ts (62 行)：按文件路径维护 promise queue，同文件操作串行化，不同文件并行。防止并行 tool 同时写一个文件。Rust 无此保护（parallel tool execution 直接并发写）。实现参考：legacy-ts/packages/coding-agent/src/core/tools/file-mutation-queue.ts。方案：在 tools/ 模块增加 per-path tokio::sync::Mutex HashMap，write/edit 工具获取锁后执行。
  - reproduce：两个并行 tool call 同时 edit 同一文件，结果可能互相覆盖
  - fix：
