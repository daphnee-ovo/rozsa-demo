---
title: TASK - batch 2026-07-28
nums: 1
---

- [x] TASK-T035: 按 sidebar 实际范围修正收起状态 hover 生命周期
  - type: fix
  - priority: P1
  - refs: 用户纠正：收起 sidebar 显示后，必须允许在整个 sidebar range 内移动和操作，离开 sidebar range 后才隐藏；不能以 18px edge trigger 作为隐藏边界
  - files:
      create: []
      modify: ["crates/rozsa-gui/frontend/app.js", "crates/rozsa-gui/src/native_split_view.rs", "crates/rozsa-gui/src/lib.rs"]
      test: ["crates/rozsa-gui/tests/gui_layout_polish_test.rs"]
  - depends_on: []
  - parallel: false
  - complexity: M
  - done_when:
      - 收起状态进入 edge trigger 后，overlay 在从 x=0 到 sidebar 实际宽度的整个 range 内保持可见并可交互，不因超过 18px trigger 宽度而隐藏
      - 指针真正离开已显示 sidebar 的 range 进入 main panel 后，overlay 才隐藏；sidebar 内的 Settings、session 和其他按钮仍可点击
      - 重挂载 sidebar WebView 产生的初始伪 pointerleave 不会隐藏刚显示的 overlay
      - 回归测试同时覆盖 edge trigger 宽度与 sidebar range 的区别、真实离开后的 hide 契约；cargo test -p rozsa-gui --test gui_layout_polish_test --offline 通过，受影响文件检查通过
