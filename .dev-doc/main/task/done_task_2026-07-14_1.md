---
title: TASK - batch 2026-07-14
nums: 9
---

- [x] TASK-T001: 独立验证 Tauri WKWebView 重挂载与 NSSplitViewController 生命周期
  - type: test
  - priority: P0
  - refs: SPEC §1 NativeSplitHost；§2 两个持久 WebView；Risks；Test Plan
  - files:
      create: ["temp/native-split-validation/Cargo.toml", "temp/native-split-validation/src/main.rs", "temp/native-split-validation/frontend/main.html", "temp/native-split-validation/frontend/sidebar.html", "docs/gui/NATIVE_SPLIT_VALIDATION.md", "temp/native-split-validation/build.rs", "temp/native-split-validation/tauri.conf.json", "temp/native-split-validation/drag-sample.txt"]
      modify: []
      test: []
  - depends_on: []
  - parallel: false
  - complexity: L
  - done_when:
      - cargo check --manifest-path temp/native-split-validation/Cargo.toml 通过；验证程序固定使用 exact Tauri 2.11.5、unstable feature，并记录实际 Tauri/Wry 版本
      - 以前台窗口运行验证程序，逐项记录两个真实 WKWebView 的 identity、parent、frame 和 Tauri bounds
      - 逐项记录连续 resize、divider 拖动、sidebar collapse、fullscreen 进入/退出、focus、中文 IME、文件拖放、devtools 和正常关闭
      - 注入安装失败，确认已创建的 WebView、constraints、split items 和 controllers 按逆序清理并恢复 single-WebView hierarchy
      - docs/gui/NATIVE_SPLIT_VALIDATION.md 区分编译检查与前台观察，并对每项标记 PASS、FAIL 或 UNVERIFIED
      - 所有项目内临时验证代码、日志和截图仅位于 temp/native-split-validation/
      - 只有全部必要项 PASS 才记录 GO；任一必要项 FAIL 时记录 NO-GO，并停止 TASK-T002 至 TASK-T009
      - 前台验证 split 安装后的原生 titlebar drag view 可拖动窗口并保留双击 zoom，且不覆盖 traffic lights、divider、页面控件或文件拖放
- [x] TASK-T002: 固定产品依赖并实现持久 NativeSplitHost
  - type: feat
  - priority: P0
  - refs: SPEC §1 NativeSplitHost；§2 两个持久 WebView；SPEC-AC-001、SPEC-AC-010、SPEC-AC-012
  - files:
      create: ["crates/rozsa-gui/src/native_split_view.rs"]
      modify: ["Cargo.toml", "Cargo.lock", "crates/rozsa-gui/Cargo.toml", "crates/rozsa-gui/src/lib.rs"]
      test: ["crates/rozsa-gui/tests/native_split_host.rs"]
  - depends_on: ["TASK-T001"]
  - parallel: false
  - complexity: L
  - done_when:
      - workspace Tauri dependency 固定为 exact =2.11.5 并启用 unstable；Cargo.lock 中 Tauri/Wry 版本与 GO 验证记录一致
      - macOS hierarchy 中安装一个 NSSplitViewController，包含一个 sidebar item 和一个 main item
      - 两个持久 WebView 分别由 child controller 与 split item 强引用，并通过 Auto Layout 填充 pane
      - AppKit 成为唯一 pane layout owner；安装后业务路径不调用两个 WebView 的 set_bounds、set_size 或 set_position
      - 安装失败按逆序清理；清理失败会报告错误并阻止启动
      - AppKit 操作限制在 main thread；AppKit 对象不进入要求 Send + Sync 的 managed state
      - crates/rozsa-gui/src/native_split_view.rs 文件头描述结构、ownership、安装/teardown 顺序并链接相关 GUI 文档
      - cargo check -p rozsa-gui 和 cargo test -p rozsa-gui --test native_split_host 通过
- [x] TASK-T003: 实现窗口级 scene router 与 revision IPC
  - type: feat
  - priority: P0
  - refs: SPEC §3 Scene 与状态边界；§4 IPC 与事件路由；SPEC-AC-005
  - files:
      create: ["crates/rozsa-gui/src/scene_router.rs"]
      modify: ["crates/rozsa-gui/src/lib.rs", "crates/rozsa-gui/src/state.rs", "crates/rozsa-gui/src/commands.rs", "crates/rozsa-gui/src/events.rs"]
      test: ["crates/rozsa-gui/tests/scene_router.rs"]
  - depends_on: ["TASK-T002"]
  - parallel: false
  - complexity: M
  - done_when:
      - set_gui_scene、gui_webview_ready、gui-scene-snapshot 的 payload 与 SPEC 一致
      - intent 串行处理，成功切换才递增 revision
      - 过期 expected_revision 返回最新完整 snapshot
      - 两个 WebView 只应用更高 revision；重复或旧 revision 不可覆盖当前状态
      - WebView 未 ready 时只保留最新 snapshot；ready 后直接获得完整 snapshot，不重放增量
      - crates/rozsa-gui/src/scene_router.rs 文件头描述状态机、revision 规则和文档链接
      - cargo test -p rozsa-gui --test scene_router 通过
- [x] TASK-T004: 拆分 sidebar/main scene 并保留非 macOS CSS fallback
  - type: refactor
  - priority: P0
  - refs: SPEC §2 两个持久 WebView；§3 Scene 与状态边界；§6 Frontend 迁移边界；SPEC-AC-002、SPEC-AC-008
  - files:
      create: ["crates/rozsa-gui/frontend/sidebar.html", "crates/rozsa-gui/frontend/sidebar.js", "crates/rozsa-gui/frontend/gui_shared.js"]
      modify: ["crates/rozsa-gui/src/lib.rs", "crates/rozsa-gui/frontend/index.html", "crates/rozsa-gui/frontend/app.js"]
      test: ["crates/rozsa-gui/tests/frontend_scene_identity.rs", "crates/rozsa-gui/tests/frontend_platform_fallback.rs", "crates/rozsa-gui/tests/native_split_host.rs"]
  - depends_on: ["TASK-T003"]
  - parallel: false
  - complexity: L
  - done_when:
      - sidebar WebView 只承载 MainSidebar/SettingsSidebar；main WebView 只承载 MainContent/SettingsContent
      - scene 切换只改变预创建 root 的 visibility/inert，不执行 reload、close、innerHTML 重建或节点替换
      - identity 测试确认 Main/Settings 往返后两个 split item、两个 WebView 和 Main stateful roots identity 不变
      - macOS frontend 不使用 CSS Grid 决定两个 native pane 宽度
      - 非 macOS 继续使用现有 CSS sidebar、main、Settings 和 collapse 路径
      - cargo test -p rozsa-gui --test frontend_platform_fallback 可重复验证平台选择逻辑
      - cargo test -p rozsa-gui --test frontend_scene_identity 通过
      - 若当前环境不能运行真实非 macOS GUI，只能把真实平台 smoke test 标记为 UNVERIFIED
- [x] TASK-T005: 实现双 WebView 定向事件路由与完整 sidebar snapshot
  - type: refactor
  - priority: P0
  - refs: SPEC §4 IPC 与事件路由；SPEC-AC-005、SPEC-AC-006
  - files:
      create: []
      modify: ["crates/rozsa-gui/src/lib.rs", "crates/rozsa-gui/src/state.rs", "crates/rozsa-gui/src/commands.rs", "crates/rozsa-gui/src/events.rs", "crates/rozsa-gui/frontend/app.js", "crates/rozsa-gui/frontend/sidebar.js", "crates/rozsa-gui/frontend/gui_shared.js"]
      test: ["crates/rozsa-gui/tests/targeted_event_routing.rs", "crates/rozsa-gui/tests/gui_event_forwarder_test.rs"]
  - depends_on: ["TASK-T004"]
  - parallel: false
  - complexity: L
  - done_when:
      - gui-scene-snapshot 在 scene/pane 变化和 WebView ready 时定向发送到 main、sidebar
      - ui-state 在 active session snapshot/stream update 时只发送到 main
      - tool-event 在 tool lifecycle 时只发送到 main
      - permission-request 在新审批请求时只发送到 main
      - sidebar-state 在 initial ready、session new/switch/rename/delete、session status、git、quota 变化时只发送到 sidebar
      - sidebar-state 是独立完整 payload，包含 session list、active session id、session activity/status、git summary、quota summary、sidebar actions 状态
      - theme-state 在 initial ready、theme 变化、translucent sidebar 变化时路由到 main、sidebar 和 native host
      - error 返回发起操作的 WebView；无法确定来源时发送到 main
      - notification 返回发起操作的 WebView；显式全局 notification 发送到两个 WebView
      - sidebar 不订阅 message streaming、tool output、permission request；main 不维护 session list 或 settings navigation 的第二份 DOM/state
      - 测试逐行覆盖 producer、target、payload、trigger，并断言 main-only/sidebar-only 事件没有重复 consumer
      - cargo test -p rozsa-gui --test targeted_event_routing 通过
- [x] TASK-T006: 保留 scene 切换期间的 session UI、focus 与 IME 状态
  - type: feat
  - priority: P0
  - refs: SPEC §3 Scene 与状态边界；SPEC-AC-004
  - files:
      create: []
      modify: ["crates/rozsa-gui/frontend/app.js", "crates/rozsa-gui/frontend/gui_shared.js"]
      test: ["crates/rozsa-gui/tests/frontend_scene_continuity.rs"]
  - depends_on: ["TASK-T005"]
  - parallel: false
  - complexity: M
  - done_when:
      - Main → Settings → Main 后 active session、draft、selection/caret、scroll、展开状态和 pending permission UI progress 保持不变
      - scene 切换发生于 IME composition 时延迟至 compositionend，preedit text 不被强制提交或丢弃
      - 返回 Main 时恢复原 focus owner 与 selection；目标不存在时明确聚焦 composer host
      - 测试覆盖普通切换、IME 中切换、目标节点消失时的 focus fallback
      - cargo test -p rozsa-gui --test frontend_scene_continuity 通过
- [x] TASK-T007: 接入原生 sidebar toggle、divider 与窗口生命周期
  - type: feat
  - priority: P0
  - refs: SPEC §5 原生窗口行为；SPEC-AC-003、SPEC-AC-007
  - files:
      create: []
      modify: ["crates/rozsa-gui/src/lib.rs", "crates/rozsa-gui/src/native_split_view.rs", "crates/rozsa-gui/src/native_titlebar.rs"]
      test: ["crates/rozsa-gui/tests/native_window_behavior.rs"]
  - depends_on: ["TASK-T005"]
  - parallel: true
  - complexity: L
  - done_when:
      - Main 与 Settings 使用同一个 titlebar action target 调用 split controller 的 toggleSidebar:
      - divider 可拖动并通过稳定 autosaveName 恢复；恢复失败使用明确初始宽度
      - sidebar min/max width、窄窗口 collapse、fullscreen overlay 由 AppKit 管理，不再使用固定 window.innerWidth 阈值
      - traffic lights、窗口拖动、双击 zoom、resize 和 fullscreen titlebar 可见性保持正常
      - 关闭窗口先解除 notification observers 和 action target，再 teardown child controllers/WebViews；pending approvals 仍被拒绝
      - crates/rozsa-gui/src/native_titlebar.rs 文件头更新 ownership/lifecycle、与 NativeSplitHost 的关系及文档链接
      - cargo test -p rozsa-gui --test native_window_behavior 通过
      - 前台 resize、divider、collapse、fullscreen 等真实 AppKit 行为留给 TASK-T009 手工验收，不以本任务自动测试替代
- [x] TASK-T008: 同步 native sidebar backing 与双 WebView theme
  - type: feat
  - priority: P0
  - refs: SPEC §4 theme-state；§5 sidebar surface contract；SPEC-AC-011
  - files:
      create: []
      modify: ["crates/rozsa-gui/src/native_split_view.rs", "crates/rozsa-gui/src/events.rs", "crates/rozsa-gui/frontend/app.js", "crates/rozsa-gui/frontend/sidebar.js", "crates/rozsa-gui/frontend/gui_shared.js", "crates/rozsa-gui/src/commands.rs", "crates/rozsa-gui/Cargo.toml"]
      test: ["crates/rozsa-gui/tests/theme_surface.rs", "crates/rozsa-gui/tests/appearance_settings_test.rs", "crates/rozsa-gui/tests/targeted_event_routing.rs"]
  - depends_on: ["TASK-T006", "TASK-T007"]
  - parallel: false
  - complexity: M
  - done_when:
      - sidebar WebView 背景保持透明
      - translucentSidebar=true 使用系统 sidebar material；关闭时使用当前主题的 opaque sidebar backing
      - theme/translucency 变化先更新 native backing，再向两个 WebView 发布同 revision theme snapshot
      - main 与 sidebar 对旧 theme revision 的处理不会覆盖新状态
      - crates/rozsa-gui/src/native_split_view.rs 文件头补充 theme backing ownership、更新顺序和文档链接
      - 测试覆盖透明、opaque、实时 theme 切换、更新顺序和旧 revision 丢弃
      - cargo test -p rozsa-gui --test theme_surface 通过
      - 真实透明度、颜色和闪烁观察留给 TASK-T009 前台验收
- [x] TASK-T009: 更新 GUI 文档并执行 native split 完整验收
  - type: test
  - priority: P0
  - refs: SPEC §7 文档与代码追踪；Test Plan；SPEC-AC-001 至 SPEC-AC-012
  - files:
      create: []
      modify: ["docs/gui/ARCHITECTURE.md", "docs/gui/TERMINOLOGY.md", "docs/gui/FRONTEND_TERMINOLOGY.md", "docs/gui/NATIVE_SPLIT_VALIDATION.md", "crates/rozsa-gui/src/native_split_view.rs"]
      test: ["crates/rozsa-gui/tests/native_split_acceptance.rs"]
  - depends_on: ["TASK-T008"]
  - parallel: false
  - complexity: L
  - done_when:
      - 文档描述一个 NSWindow、一个 NativeSplitHost、两个持久 WebView、scene router 和 targeted IPC
      - 文档不再把单 WebView 双 CSS sidebar 描述为当前 macOS 实现
      - 文档链接回 native_split_view.rs、native_titlebar.rs 和 scene/frontend anchors，完成双向追踪
      - cargo check -p rozsa-gui 通过
      - cargo test -p rozsa-gui --test native_split_acceptance 通过
      - cargo test -p rozsa-gui 通过
      - 在真实前台 .app 中逐项验证普通、窄窗口、最大化、fullscreen，以及 sidebar 展开/折叠组合
      - 逐项验证 WebView/split item identity、divider persistence、focus、中文 IME、文件拖放、devtools、traffic lights、窗口拖动、双击 zoom、关闭和 pending approval 拒绝
      - 逐项验证 transparent/opaque sidebar、实时 theme 切换和无可见 backing 闪烁
      - 所有手工项目在验证文档中记录 PASS、FAIL 或 UNVERIFIED；自动测试通过不能替代真实 AppKit/WKWebView 观察
      - 非 macOS 定向 fallback 测试结果单独记录；没有对应运行环境时真实非 macOS GUI 明确标记 UNVERIFIED
      - Dark 主题下 translucent sidebar 使用 DarkAqua material，文字与背景保持可读对比度
