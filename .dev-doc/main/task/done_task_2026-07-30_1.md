---
title: TASK - batch 2026-07-30
nums: 1
---

- [x] TASK-T045: Refine permission rule editor and matchers
  - type: feat
  - priority: P0
  - refs: User permission settings follow-up 2026-07-30
  - files:
      create: []
      modify: ["crates/rozsa-app/src/permissions/mod.rs", "crates/rozsa-app/src/settings/schema.rs", "crates/rozsa-app/src/settings/merge.rs", "crates/rozsa-app/src/settings/storage.rs", "crates/rozsa-app/tests/permissions_test.rs", "crates/rozsa-app/tests/settings_capabilities_test.rs", "crates/rozsa-gui/src/commands.rs", "crates/rozsa-gui/frontend/app.js", "crates/rozsa-gui/frontend/index.html", "crates/rozsa-gui/tests/settings_capabilities.rs", "docs/gui/UI_USAGE_GUIDELINES.md", "docs/gui/FRONTEND_TERMINOLOGY.md", "docs/gui/ARCHITECTURE.md", "docs/cli/README.md", "crates/rozsa-gui/src/lib.rs"]
      test: []
  - depends_on: []
  - parallel: false
  - complexity: L
  - done_when:
      - Permission mode shows only on-request auto-approve and yolo while displaying the effective value for each scope
      - Each Add rule editor renders inside its deny ask or allow container
      - Rule editor combines tool selection and highlighted pattern text using the existing rich-input token component
      - Path glob supports standard star and double-star examples including *.md **/*.md docs/*.md and docs/**/*.md
      - Global path rules require a literal $HOME/ prefix and cannot escape the home boundary after normalization
      - Optional RegExp mode is validated uses full matching and applies to normalized path or individual shell segment
      - Default global allow rules are ls(*) grep(*) find(*) subagent(*) and askUserQuestion(*) and replace hidden built-in allowances
      - Regression tests docs full workspace checks and deployed GUI verification pass
      - Changes are committed in one dedicated commit
      - Rule rows can be dragged atomically between deny ask and allow containers
      - The universal rule *(*) is rejected by frontend and backend; yolo is the explicit all-tools bypass mode
      - Tool uses an editable single-select combobox with prefix filtering and Tab completion
      - Rule rows display the persisted form on one line as ToolName(pattern)
      - Global path editors show a fixed $HOME/ prefix
      - Cross-list dragging works in the actual WKWebView through pointer interaction
