---
title: TASK - batch 2026-07-12
nums: 1
---

- [x] TASK-T007: 统一 Appearance 界面设置并接入可持久化主题系统
  - type: feat
  - priority: P0
  - refs: 用户需求：将界面显示设置统一放入 Appearance；Theme Mode system/light/dark；Font Size 5-50px 滑条与输入框；Light/Dark Theme 默认 Rozsa/Rozsa Dark，支持自定义主题文件 ~/.rozsa/themes/<theme_name>.json；UI 只暴露 Accent、Background、Foreground、UI font、macOS translucent sidebar、Code font，其他主题变量走文件配置。
  - files:
      create: ["crates/rozsa-app/src/themes.rs", "crates/rozsa-app/tests/theme_test.rs", "crates/rozsa-gui/tests/appearance_settings_test.rs", "docs/app/themes.md"]
      modify: ["crates/rozsa-app/src/settings/schema.rs", "crates/rozsa-app/src/settings/merge.rs", "crates/rozsa-app/src/settings/mod.rs", "crates/rozsa-app/src/settings/storage.rs", "crates/rozsa-gui/src/commands.rs", "crates/rozsa-gui/src/lib.rs", "crates/rozsa-gui/frontend/index.html", "crates/rozsa-gui/frontend/app.js"]
      test: ["crates/rozsa-app/tests/theme_test.rs", "crates/rozsa-gui/tests/appearance_settings_test.rs"]
  - depends_on: []
  - parallel: false
  - complexity: L
  - done_when:
      - 设置面板存在独立 Appearance tab，通用设置中不再重复显示界面显示项。
      - Theme Mode 的 system、light、dark 均能切换并实际改变当前 CSS 变量；system 会响应系统主题变化。
      - Font Size 提供 5-50px 滑条和数值输入框，双向同步、边界校验、重启后保留。
      - Light Theme 与 Dark Theme 都提供 Rozsa 默认主题和 ~/.rozsa/themes/<theme_name>.json 自定义主题选择；主题文件加载失败会明确报错，不静默使用空配置。
      - Accent、Background、Foreground、UI font、macOS translucent sidebar、Code font 六项均能在 UI 修改并立即生效；translucent sidebar 在非 macOS 上明确禁用或不显示。
      - 主题配置中除 UI 暴露项外的额外 CSS 变量可由 JSON 文件配置并实际应用。
      - 主题模式、字体大小、主题选择和可见主题项均通过后端持久化，不依赖仅存在于 WebView localStorage 的状态。
      - 新增 Rust 与 GUI 回归测试覆盖主题解析、默认值、无效值、5-50px 边界和 Appearance IPC 数据。
      - 相关主题配置格式和运行行为已记录在 docs/app/themes.md。
