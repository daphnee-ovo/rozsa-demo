---
title: TASK - batch 2026-08-02
nums: 1
---

- [x] TASK-T025: Secure model API key configuration with private Rózsa environment
  - type: feat
  - priority: P1
  - refs: User request: forbid shell-command API keys, require $ environment references, migrate plaintext apiKey values to ~/.rozsa/.env, and keep those variables Rózsa-only.
  - files:
      create: ["crates/rozsa-model/tests/credentials_config_test.rs"]
      modify: ["crates/rozsa-model/src/credentials.rs", "crates/rozsa-app/src/model_registry/mod.rs", "crates/rozsa-app/src/agent_session.rs", "crates/rozsa-app/src/subagent/manager.rs", "crates/rozsa-app/tests/model_registry.rs", "docs/model/models-config.md", "docs/app/README.md", "crates/rozsa-model/src/env_keys.rs", "crates/rozsa-app/Cargo.toml", "Cargo.lock"]
      test: ["crates/rozsa-model/tests/credentials_config_test.rs", "crates/rozsa-app/tests/model_registry.rs"]
  - depends_on: []
  - parallel: false
  - complexity: L
  - done_when:
      - Model and header credential references beginning with ! are rejected with an explicit error and no shell process is started.
      - Only values beginning with $ are resolved as environment-variable references; variable names are validated, while other non-empty values remain literal credentials.
      - Rózsa reads a private global ~/.rozsa/.env file without exposing its values to unrelated shell configuration, while explicit process environment values remain usable.
      - First load of a file-backed model config migrates plaintext provider apiKey values into the private .env file, replaces them with a generated $ reference, writes atomically with restrictive permissions, and is idempotent without leaving the secret in the model config.
      - Main and subagent model requests resolve configured apiKey references through the new rules, and focused regression tests cover shell rejection, $ references, private env loading, and plaintext migration.
      - Model configuration and app/model documentation describe the new syntax, migration, private .env location, and removal of shell-command support.
