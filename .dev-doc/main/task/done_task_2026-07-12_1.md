---
title: TASK - batch 2026-07-12
nums: 6
---

- [x] TASK-T001: 展开 permission 请求详情并优化命令展示
  - type: fix
  - priority: P0
  - refs: 用户请求：permission display 调整；参考附件中的 Bash/tool name、description、展开全部命令和 bash 高亮要求。
  - files:
      create: []
      modify: ["crates/rozsa-gui/frontend/app.js", "crates/rozsa-gui/frontend/index.html", "crates/rozsa-gui/src/state.rs", "crates/rozsa-gui/src/events.rs", "crates/rozsa-cli/src/run.rs"]
      test: ["crates/rozsa-gui/tests/session_event_routing_test.rs"]
  - depends_on: []
  - parallel: false
  - complexity: S
  - done_when:
      - 审批面板显示 tool name、description 和命令内容；命令默认折叠，点击展开后可查看全部内容。;长命令不因 CSS 溢出而隐藏关键信息；Bash 命令保持可读的语法和参数区分。;cargo test -p rozsa-gui --test permission_display_test 与 rtk node --check crates/rozsa-gui/frontend/app.js 通过。
- [x] TASK-T002: 放行无害读取命令并收紧文件读取授权边界
  - type: fix
  - priority: P0
  - refs: 用户请求：head、tail、cat、grep、sort 等完全无害命令免申请；echo 等有副作用命令仍需授权；项目目录内 Read 默认放行，超出项目目录才触发授权。
  - files:
      create: []
      modify: ["crates/rozsa-app/src/permissions/mod.rs"]
      test: ["crates/rozsa-app/tests/permissions_test.rs", "crates/rozsa-app/tests/permission_safe_commands_test.rs"]
  - depends_on: []
  - parallel: false
  - complexity: L
  - done_when:
      - Bash 的 head、tail、cat、grep、sort 及安全参数组合自动放行；echo、写入重定向、管道到有副作用命令以及网络、写入、删除命令仍需审批。;项目目录内 Read 自动放行；项目目录外 Read 产生 NeedApproval；路径穿越、绝对路径和 symlink 边界有测试覆盖。;cargo test -p rozsa-app --test permissions_test 与 cargo test -p rozsa-app --test permission_safe_commands_test 通过。
- [x] TASK-T003: 让 context window 统计真实并在阈值触发 compaction
  - type: fix
  - priority: P0
  - refs: 用户请求：context window 显示必须真实；达到窗口上限时必须触发 compaction，而不是长期显示 100% 仍继续运行。
  - files:
      create: []
      modify: ["crates/rozsa-app/src/agent_session.rs", "crates/rozsa-app/src/compaction/mod.rs", "crates/rozsa-gui/src/commands.rs", "crates/rozsa-gui/frontend/app.js"]
      test: ["crates/rozsa-app/tests/context_window_test.rs", "crates/rozsa-gui/tests/context_window_display_test.rs"]
  - depends_on: []
  - parallel: false
  - complexity: L
  - done_when:
      - UI 使用当前模型 context_window 与真实累计 token usage 计算百分比，不使用固定或伪造窗口值。;达到 compaction 阈值时 AgentSession 触发可观察的 compaction 事件并继续处理；compaction 失败显式报错。;cargo test -p rozsa-app --test context_window_test 与 cargo test -p rozsa-gui --test context_window_display_test 通过。
- [x] TASK-T004: 修复 thinking 展示抖动并支持点击查看内容
  - type: fix
  - priority: P0
  - refs: 用户请求：thinking 框不应持续抽搐；thinking 过程可点击展开查看，流式更新不应破坏展开状态。
  - files:
      create: []
      modify: ["crates/rozsa-gui/frontend/app.js", "crates/rozsa-gui/frontend/index.html"]
      test: ["crates/rozsa-gui/tests/thinking_display_test.rs"]
  - depends_on: []
  - parallel: false
  - complexity: M
  - done_when:
      - thinking 流式更新只更新内容，不重复创建或替换整个 thinking 容器；动画状态稳定。;thinking 容器可点击展开和收起，展开后显示已收到的完整内容；后续 token 不自动收起。;cargo test -p rozsa-gui --test thinking_display_test 与 rtk node --check crates/rozsa-gui/frontend/app.js 通过。
- [x] TASK-T005: 持久化并传播默认 cwd 与 rozsa 路径参数
  - type: fix
  - priority: P0
  - refs: 用户请求：启动时确定初始化 cwd；cwd 持久化；agent 使用 cd 后下一次继承；rozsa 后接路径参数时使用该路径。
  - files:
      create: []
      modify: ["crates/rozsa-cli/src/args.rs", "crates/rozsa-cli/src/run.rs", "crates/rozsa-app/src/agent_session.rs", "crates/rozsa-app/src/session/manager.rs", "crates/rozsa-gui/src/state.rs", "crates/rozsa-gui/src/commands.rs"]
      test: ["crates/rozsa-cli/tests/working_directory_test.rs", "crates/rozsa-app/tests/working_directory_persistence_test.rs", "crates/rozsa-gui/tests/working_directory_test.rs"]
  - depends_on: []
  - parallel: false
  - complexity: L
  - done_when:
      - 无路径参数时使用并持久化启动时 process cwd；rozsa 后接路径参数时使用 canonicalized path，并让 GUI、session、tools 使用该 path。;Agent 执行成功的 cd 后更新当前 session cwd；重新启动或恢复 session 时继承更新后的 cwd。;相对路径、绝对路径、无效路径和恢复 session 有测试；对应 cargo test 命令全部通过。
- [x] TASK-T006: 修复 GUI 输入法 composition 连续输入
  - type: fix
  - priority: P0
  - refs: 用户请求：中文输入法组合输入不能被每个 keydown 打断；y 后再输入 i 时必须保持同一次 composition，预览显示 yi。
  - files:
      create: []
      modify: ["crates/rozsa-gui/frontend/app.js", "crates/rozsa-gui/frontend/index.html"]
      test: ["crates/rozsa-gui/tests/ime_input_test.rs"]
  - depends_on: []
  - parallel: false
  - complexity: M
  - done_when:
      - 输入框实现 compositionstart、compositionupdate、compositionend；composition 期间不触发普通 keydown 的发送、重渲染或 selection 重置。;中文拼音 yi 等连续组合输入保持同一次预览；compositionend 后文本、caret、slash 和 @ 补全状态正确。;cargo test -p rozsa-gui --test ime_input_test 与 rtk node --check crates/rozsa-gui/frontend/app.js 通过。
