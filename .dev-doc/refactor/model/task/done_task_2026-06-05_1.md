---
title: TASK - 模型可用性筛选迁移到 Rust（方案B）
nums: 4
---

- [x] TASK-T001: Rust 侧实现 provider_available 计算
  - type: feat
  - priority: P0
  - refs: user-request
  - files:
      create: []
      modify: ["crates/rozsa-app/src/model_registry/mod.rs", "crates/rozsa-model/src/env_keys.rs"]
      test: ["tests/unit/model/rozsa-model-bridge.test.ts"]
  - depends_on: []
  - parallel: true
  - complexity: M
  - done_when:
      - ModelRegistry 新增 `provider_available()` 方法，返回 HashMap<String, ProviderAvailable>
      - ProviderAvailable 包含 configured: bool 和 source 字段（"environment" / "models_json_key" / "models_json_command"）
      - 环境变量检查复用现有 env_keys.rs 的 get_env_api_key
      - models.json 中字面 apiKey（非 ! 开头）和 env var 引用能正确识别为 configured
      - models.json 中 `!command` 形式标记为 source="models_json_command"（configured=true 但不执行命令）

- [x] TASK-T002: rozsa-app bridge 响应中增加 provider_available 字段
  - type: feat
  - priority: P0
  - refs: user-request
  - files:
      create: []
      modify: ["crates/rozsa-app/src/main.rs"]
      test: ["tests/unit/model/rozsa-model-bridge.test.ts"]
  - depends_on: [TASK-T001]
  - parallel: false
  - complexity: S
  - done_when:
      - list_models 响应从 `{type, id, models, errors}` 扩展为 `{type, id, models, provider_available, errors}`
      - provider_available 为 Record<provider, {configured, source}> 的 JSON 对象
      - TS 侧 rust-model-registry.ts 解析新字段并传递给 ModelRegistry

- [x] TASK-T003: TS 侧 ModelRegistry.getAvailable() 改用 Rust 返回的 provider_available
  - type: refactor
  - priority: P0
  - refs: user-request
  - files:
      create: []
      modify: ["packages/coding-agent/src/core/model-registry.ts", "packages/coding-agent/src/core/rust-model-registry.ts"]
      test: ["packages/coding-agent/test/model-registry.test.ts"]
  - depends_on: [TASK-T002]
  - parallel: false
  - complexity: M
  - done_when:
      - rust-model-registry.ts 解析 bridge 返回的 provider_available 字段
      - ModelRegistry 在 Rust backend 模式下，hasConfiguredAuth 优先使用 Rust 返回的 provider_available
      - OAuth 认证仍由 TS 侧 AuthStorage 补充（Rust 返回 configured=false 但 TS 侧有 OAuth token 时仍视为可用）
      - getAvailable() 逻辑不变：先查 Rust provider_available，再查 TS OAuth，再查 providerAvailableIds 探测
      - backend=ts 时退回原有纯 TS 逻辑

- [x] TASK-T004: 更新文档
  - type: docs
  - priority: P1
  - refs: user-request
  - files:
      create: []
      modify: ["docs/model/supported-providers.md"]
      test: []
  - depends_on: [TASK-T003]
  - parallel: false
  - complexity: S
  - done_when:
      - supported-providers.md 说明 auth 检查现在由 Rust 侧完成（env var + models.json 字面 key）
      - 说明 OAuth 仍由 TS 管理的原因
      - 说明 backend=ts 时的退回行为

