# Rozsa 视觉设计规范

## 设计定位

Rozsa 是一套面向长时间工作的编码代理工作台。它以暖浆纸为画布、复古墨色为文字、低饱和玫红为动作信号：吸收 Apple 的空间秩序与轻薄层级，吸收 Claude 的温和编辑感，再用海报式版心和留白建立自己的识别度。

**核心原则：内容像被认真排版的海报一样清楚，操作像可靠的工具一样安静。**

海报感只用于空状态、场景标题和重要分组的构图；消息、代码、工具参数和设置表单优先保证阅读、复制与操作效率。

## 颜色令牌

所有界面颜色从令牌派生，组件中不散落新的色值。`--accent` 是唯一品牌强调色；成功、警告、错误等颜色只承担语义，不承担装饰。

### 浅色主题

```css
:root {
  --bg:      oklch(96.8% 0.018 82);
  --surface: oklch(98.6% 0.010 82);
  --fg:      oklch(24% 0.026 55);
  --muted:   oklch(51% 0.024 68);
  --border:  oklch(87.5% 0.022 78);
  --accent:  oklch(58% 0.075 355);
}
```

| 令牌 | 角色 |
| --- | --- |
| `--bg` | 暖浆纸底，应用窗口、侧栏外层和设置页背景 |
| `--surface` | 干净浅纸面，主工作区、消息流和输入面 |
| `--fg` | 带暖度的复古墨色，标题、正文和主要控件文字 |
| `--muted` | 暖灰墨色，说明、日期、次要状态和占位文字 |
| `--border` | 低对比纸纤维色，分隔线和输入边界 |
| `--accent` | 柔和低饱和玫红，主动作、当前选择、焦点和少量链接 |

### 暗色主题

暗色主题是同一套系统的“暖墨夜间版”，只替换表面与文字关系，不改变形状、间距和交互语义。

```css
[data-theme="dark"] {
  --bg:      oklch(18% 0.018 55);
  --surface: oklch(23% 0.022 55);
  --fg:      oklch(92% 0.018 78);
  --muted:   oklch(72% 0.022 72);
  --border:  oklch(35% 0.022 55);
  --accent:  oklch(70% 0.085 355);
}
```

暗色代码面和工具输出面可以比 `--surface` 更深，但不能使用冷蓝黑或纯黑作为大面积底色。暗色主按钮使用暖墨文字配较亮玫红；浅色主按钮使用纸白文字配中等明度玫红。

### 派生色

- `--accent-soft`：`color-mix(in oklch, var(--accent) 12–16%, transparent)`，用于选中底、用户消息便签底和焦点环；用户消息的玫红面保持半透明、低饱和。
- `--fg-soft`：`color-mix(in oklch, var(--fg) 5–7%, transparent)`，用于悬停底和极轻表面变化。
- 语义色使用低饱和的青绿、琥珀和砖红；必须同时配合状态文字或图标，不能只靠颜色传达意义。
- 正文目标对比度至少 `4.5:1`，大字和图标至少 `3:1`。悬停、选中和聚焦状态的文字对比度不得低于默认状态。

## 字体系统

Rozsa 使用三类字体，分别承担不同信息职责：

| 职责 | 字体栈 | 用途 |
| --- | --- | --- |
| 展示衬线 | `Iowan Old Style, Baskerville, "Songti SC", "Noto Serif CJK SC", STSong, Georgia, serif` | 空状态、场景标题、设置页标题、海报式短句 |
| 界面无衬线 | `-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Noto Sans CJK SC", "Segoe UI", system-ui, sans-serif` | 导航、正文、按钮、标签和表单 |
| 等宽 | `"JetBrains Mono", "Cascadia Code", "Fira Code", ui-monospace, SFMono-Regular, Menlo, monospace` | 代码、命令、工具参数、快捷键、计数和版本 |

- `display-xl`：`48–64px / 1.05–1.12`，用于独立空状态或场景主标题。
- `display-lg`：`32–44px / 1.10–1.18`，用于页面标题和主要分组。
- `title`：`20–24px / 1.25–1.35`，用于工作区、会话和对话框标题。
- `body`：`15–17px / 1.50–1.65`，用于消息正文与说明。
- `label`：`13–14px / 1.35–1.50`，用于导航、按钮和状态。
- `caption`：`11–12px / 1.35–1.50`，用于时间、来源和辅助信息。
- `code`：`13–14px / 1.55–1.70`，用于代码和工具输出。

标题允许轻微负字距，但必须自然换行；不得用 `white-space: nowrap` 换取视觉整齐。中文标题要避免末行只剩一两个字。界面默认文案使用 **English**，品牌名、命令、代码、模型名和技术标识保持原样。

## 材质与表面

### 暖浆纸与原木感

- 暖度来自 `--bg` 的轻微黄棕色相，不做明显复古滤镜。
- 纹理只表现为低对比纸纤维或微颗粒，透明度通常控制在 `2%–4%`；正文和代码区域使用更低强度。
- 不使用明显木结、木板纹、撕边、强噪点或装饰性摄影。纹理不能影响文字、选中态和代码高亮。
- 暗色主题可保留极低强度颗粒，不使用浅色木纹；深度由暖墨表面、表面差和阴影表达。

### 表面层级

| 层级 | 表达 | 对象 |
| --- | --- | --- |
| `L0` | 平面排版，无阴影 | 页面、消息流、普通列表 |
| `L1` | 轻边界或极轻阴影 | 活跃会话、输入框、普通内容块 |
| `L2` | 半透明表面、柔和阴影、`12–16px` 背景模糊 | 顶栏、浮动输入区、菜单和 popover |
| `L3` | 更明确的扩散阴影、遮罩和 `16–24px` 背景模糊 | 权限请求、用户问题和设置对话框 |

阴影优先扩大模糊半径，减少垂直位移：

- `shadow-1`：`0 2px 10px oklch(24% 0.026 55 / 0.06)`。
- `shadow-2`：`0 8px 28px oklch(24% 0.026 55 / 0.10)`。
- `shadow-3`：`0 18px 54px oklch(24% 0.026 55 / 0.16)`。

只有真正悬浮、固定或需要脱离内容流的对象使用阴影和模糊。内容流、代码块、工具输出和普通消息不使用背景模糊；模糊必须配合足够不透明度、边界或阴影。

## 圆角与间距

使用 `8px` 基础间距节奏，紧凑控件可使用 `4px` 子步长。所有组件从固定且克制的圆角阶梯中取值，默认不在相邻层级之间插值：

| 令牌 | 值 | 对象 |
| --- | ---: | --- |
| `--radius-xs` | `8px` | 代码标签、键盘按键、微型状态标记 |
| `--radius-control` | `12px` | 按钮、输入框、选择器和菜单项 |
| `--radius-card` | `16px` | 会话项、工具块、普通内容卡片 |
| `--radius-panel` | `20px` | 输入面板、侧栏内面和工作区面板 |
| `--radius-dialog` | `24px` | 权限请求、问题面板和设置对话框 |
| `--radius-pill` | `999px` | 少量状态标签和筛选器 |

同类组件始终使用同一半径；同一导航栏、标签组或工具栏中的同级控件统一使用 `--radius-control`，不为外层容器和内部控件叠加不同的胶囊形状。若需容纳整组表面，外层最多使用 `--radius-panel`，并避免嵌套胶囊。小尺寸图形和控件默认采用更饱满的 `--radius-xs` 或 `--radius-control`，不使用过小的尖锐圆角；只有状态点、圆形图标按钮等具有明确圆形语义的对象才使用圆形。若需区分层级，优先使用间距、表面、边界和阴影，而不是继续增加圆角种类。圆角是结构语言，不是每个元素的装饰；长文本、表格和代码容器不使用胶囊圆角。

## 版式与海报构图

### 工作台骨架

- 桌面端保持“侧栏 + 主工作区”结构，侧栏约 `240–304px`。
- 消息正文控制在 `680–820px` 的阅读列内；代码和工具输出可以在独立容器中扩展。
- 顶栏、消息流和输入区形成稳定的上 / 中 / 下三段结构；浮动输入区不能遮挡消息末端。
- `760px` 以下侧栏转为可召回浮层，主区重排为单列；页面不得产生横向滚动。
- 视口变化时，标题、内边距、间距和局部宽度使用有上下限的自适应尺度；窄屏优先重排而不是挤压，交互目标仍尽量保持 `44 × 44px`。

### 海报构图

- 使用 8 或 12 列版心，让标题块、内容块和留白形成不完全对称的秩序。
- 每个画面只保留一个主要视觉焦点：标题、真实产品状态或关键内容三者不同时争夺中心。
- 通过大留白、衬线标题、细分隔线、少量玫红印记和明确边缘建立海报感。
- 玫红印记可以是小色块、编号、状态点或细线，不使用大面积渐变、漂浮贴纸或装饰插画。
- 空状态更像展签：一个标题、一句具体说明、一个主要动作和少量状态信息。
- 活跃会话、工具调用和设置表单保持可预测的位置与操作路径。

## 组件规则

### 图标与操作

- 所有界面图标必须统一使用 Lucide 图标库中的图标，保持线条、比例和视觉风格一致；不得混用其他图标库或自绘功能图标。
- 能直接表达语义的操作优先使用简约线性图标；无法独立表达含义时，再使用“图标 + 文字”，不要为了装饰给每个标题添加图标。
- 图标容器必须在水平和垂直方向保持居中，SVG 使用独立的块级尺寸，避免基线偏移或视觉漂移。
- 图标按钮的可操作区域尽量达到 `44 × 44px`；仅图标操作必须提供 `aria-label`、`title` 或等效的无障碍名称。禁止使用 emoji 代替功能图标。

### 导航与应用壳

- Top bar 属于 `L2`：半透明纸面、`12–16px` 模糊和柔和阴影；品牌名可使用小号衬线。
- Sidebar 接近 `--bg`，不做高对比卡片墙。会话项默认平面，当前项使用 `accent-soft`、轻边界和文字权重表达。
- 会话项使用 `--radius-control`。悬停改变表面或边界，不降低文字对比度。

### 消息、工具与代码

- Assistant message 默认是 `L0` 连续正文流，不为每条消息添加头像、边框或阴影卡片；与用户便签之间用留白建立节奏。
- User message 是单一的矩形便签：左对齐，保持直角（`0` 圆角），使用半透明、低饱和玫红纸面和轻柔扩散阴影，整体只允许轻微旋转来表达未水平贴放；不添加第二层边、撕边、折角或额外便签。
- 用户便签与相邻 Agent message 之间保留明确的 padding，形成语义对比而不是装饰堆叠。正文使用展示衬线，首字母可使用克制的下沉大写；元信息仍使用等宽小字。
- Thinking block 是可折叠的 `L1` 内容块；展开与收起必须有文字或图标提示，不只依赖颜色。
- Tool call 使用编辑列表或细轨道：状态、工具名、参数摘要和输出按层级排列，不把每个调用做成厚重卡片。
- Code block 使用暖墨深面和等宽字体；复制操作放在明确但安静的工具区，横向滚动只发生在代码容器内。

### 输入、浮层与设置

- Composer 是工作区主要的 `L2` 浮动面：`--radius-panel`、暖纸 / 暖墨表面、轻模糊和柔和阴影。
- 每个视口默认只有一个主按钮；发送、确认等主要动作使用玫红，其他控件使用边界、文字或图标层级。
- Permission request、User question 和设置对话框属于 `L3`：标题、说明、操作顺序明确；一个主要动作配一个低强调次要动作。
- Form group 使用连续纸面和明确分组标题，不把每一项设置拆成独立浮卡。
- Badge 和状态标签少量使用 `--radius-pill`，不能把所有元信息胶囊化。

## 状态与可访问性

| 状态 | 反馈方式 |
| --- | --- |
| 默认 | 暖纸面、复古墨色和轻边界 |
| 悬停 | 表面明度、边界或轻微位移变化；文字颜色不变浅 |
| 聚焦 | 至少 `2px` 的清晰玫红焦点环 |
| 选中 | `accent-soft`、边界和文字权重组合，不能只靠颜色 |
| 按下 | 轻微位移或缩放，不改变布局 |
| 运行 | 状态文案、状态点或短暂动效，不持续大面积闪烁 |
| 禁用 | 降低饱和度和反馈，但保留轮廓与可读标签 |
| 错误 / 警告 / 成功 | 语义色 + 文字 + 图标的组合 |

可操作目标尽量达到 `44 × 44px`。所有可聚焦元素必须有 `:focus-visible`。阴影、纹理和模糊不能成为识别层级的唯一依据。长路径、命令和代码可以在独立容器内滚动，页面本身不可横向滚动。

## 视觉克制

- 玫红在单个画面中只出现于少数关键位置，默认不同时染色标题、图标、状态点和所有按钮。
- 不使用紫色渐变、冷蓝灰主背景、纯黑大面积导航、功能性 emoji 或泛化营销插画。
- 不给每个元素同时叠加纹理、阴影、模糊和大圆角；每种效果都必须解释真实的层级或材质关系。
- 内容优先于装饰：展示真实消息、工具、代码和状态，不用无意义的指标或占位文案填充版面。
- 浅色与暗色必须共享形状、间距、字体职责和状态语义；暗色不是另一套品牌风格。
- 分割线仅用于必要边界；同一局部不连续使用多条分割线，优先用留白、表面差异和阴影表达层级。
- 能由图标直接表达语义的操作，优先使用简约图标；需要补充含义时使用图标加文字。图标应在容器内保持水平、垂直居中，图标按钮必须提供 `aria-label` 或等效的无障碍名称。
- 推荐操作可克制地使用展示衬线字体强调，但不扩展为所有按钮的通用样式。
